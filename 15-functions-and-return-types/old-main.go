package main

import (
	"fmt"
)

var deckSize int

func main() {
	// var card string
	// card = "Ace of spades"

	// var card string = "Ace of spades"
	// var card = "Ace of spades"
	// := is only for new declaration and initialisation
	// assigning new values only needs
	card := "Ace of spades"
	fmt.Println(card)

	// deckSize = 50
	// fmt.Println(deckSize)
}
